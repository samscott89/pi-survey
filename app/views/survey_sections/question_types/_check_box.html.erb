<%= f.label q.subtext, class: "control-label" %>
<% q.question_options.each do |opt| %>
<div class="checkbox">
	<%= f.label "option_id_#{opt.id}" do %>
	<%= f.check_box :option_id, {multiple: true}, opt.id, nil %> <%= opt.option_choice.choice_name.capitalize %>
	<% end %>
</div>
<% end %>