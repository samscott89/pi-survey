<% provide(:title, 'Campaign Participants') %>
<div class="row">
<div class="col-md-offset-1 col-md-10">


<div class="page-header">
<h1><%= @campaign.name %> <br><small>Participants </small></h1>
</div>

<%= link_to "Add participants", add_campaign_participants_path(@campaign), class: "btn btn-primary" %>

<hr>

<%= paginate @participants %>

<div class="list-group">
  <%= render @participants %>
</div>

<%= paginate @participants %>

</div>
</div>
