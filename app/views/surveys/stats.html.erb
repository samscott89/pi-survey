<%= provide(:title, @survey.name) %>

<div class="center hero-unit">

<h1><%= @survey.name %></h1>
	<div class="table-responsive">
	  <table class="table table-hover">
	    <thead>
	    <tr>
	    	<th>User</th>
	    	<% @questions.each do |q| %>
	    		<th><%= q.id %> <%= q.subtext %></th>
	    	<% end %>
	    </tr
	    </thead>
	    <tbody>
	    	<% @users.each do |u| %>
	    	<tr>
	    		<th> <%= u.id %> </th>
	    		<% @questions.each do |q| %>
	    			<td><%= @answers[u.id][q.id].join(" ") unless @answers[u.id][q.id].nil? %></td>
	    		<% end %>
	    	</tr>
	    	<% end %>
	    </tbody>
	  </table>
	</div>

	<%= link_to "Export as CSV",  survey_stats_path(@survey, format: :csv), class: "btn btn-large btn-success"%>

</div>